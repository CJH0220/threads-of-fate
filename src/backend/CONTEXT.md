# 领域术语表

> 本文件是《命运的织线》后端子系统的领域语言定义。纯术语，不含实现细节。
> 更新规则：每当一个新术语在讨论中达成共识，立即写入此处。

---

## 时间

| 术语 | 英文 | 定义 |
|------|------|------|
| **时段** | Slot | 一天中的三个时间片之一：早晨（morning）、正午（noon）、夜晚（night） |
| **天** | Day | 游戏内的一天，范围 1–60。一局共 60 天 |
| **周** | Week | 7 天为一周，从 day 派生：`ceil(day / 7)`，共 9 周 |
| **周阶段** | Week Phase | 每周的叙事阶段名称（如"旧神将熄""外来者入潮"），用于事件池选择和气氛注入 |
| **时间推进** | Time Advance | 从当前时段移动到下一个时段的操作。是核心循环的触发器 |
| **终局** | Endgame | day=60 night 结算后触发的结局判定阶段。此后时间不再推进 |

## 资源

| 术语 | 英文 | 定义 |
|------|------|------|
| **香火** | Incense | 土地公的信仰资源。初始 50，每日 -1，60 天后需 ≥100 维持神位 |
| **神力** | Divine Power | 玩家干预世界的能力。初始上限 10，每日回满，上限可通过周结算动态调整（5–20） |
| **阴德** | Yin De | 黑暗路线的道德积分。只增不减。≥100 + 香火≥100 + 阳德<100 → 邪神结局 |
| **阳德** | Yang De | 光明路线的道德积分。只增不减。≥100 + 香火≥100 + 阴德<100 → 天界善神结局 |

## 关系与命运

| 术语 | 英文 | 定义 |
|------|------|------|
| **缘线** | Bond/Thread | NPC 之间的人际关系，包含颜色（红/金/蓝/灰/黑）、强度（0–100）、活跃度（0–100） |
| **业线** | Karma Line | NPC 的人生目标线。每 NPC 有 1 条主线 + 2–3 条支线，各含 5–10 个节点，进度 0–100% |
| **命运硬币** | Fate Coin | 事件结算的随机判定机制。角色能力值 vs 事件需求值，抛硬币决定成败 |
| **命运线** | Destiny Line | 业线 + 缘线的统称，在 UI 上以双图层可视化展示 |

## NPC 结构

| 术语 | 英文 | 定义 |
|------|------|------|
| **静态层** | Static Layer | NPC 不可变属性：姓名、年龄、背景、四围属性（心智/信仰/体魄/魅力）、五维性格 |
| **动态层** | Dynamic Layer | NPC 每时段可变状态：位置、幸福度、精力、情绪、当前目标、业线进度 |
| **记忆层** | Memory Layer | NPC 持续积累的记忆：事件链、关键记忆、情感印象、语义记忆 |
| **Agent 分级** | Agent Tier | NPC 的 AI 成本分级：S（完整 LLM Agent）、A（半完整）、B（规则驱动）、C（轻量剧情） |

## 事件

| 术语 | 英文 | 定义 |
|------|------|------|
| **事件模板** | EventTemplate | 预定义的事件蓝图：触发条件（时间/地点/参与者）+ 多个可能结果 |
| **结果** | Outcome | 事件的一个可能分支，包含资源/缘线/业线/NPC状态/小镇状态的增量指令 |
| **锚点事件** | Anchor Event | 权重≥1000 的强制触发事件，构成叙事骨干（如天界通知、终局天灾） |
| **关键事件** | Key Event | 权重 3-5 的中等概率事件，玩家干预可影响走向 |
| **日常事件** | Daily Event | 权重 1-3 的低风险气氛事件 |
| **事件匹配** | Event Matching | 每时段按时间/地点/参与者 + 权重概率筛选要触发的事件 |
| **事件结算** | Event Settlement | 执行匹配事件的结果：应用增量、记录记忆、分发后续事件 |

## 存档

| 术语 | 英文 | 定义 |
|------|------|------|
| **存档槽位** | Save Slot | 0=自动档, 1-3=手动档。每个槽位对应一个 JSON 文件 |
| **GameSession** | GameSession | 5 模块运行时容器，存档的序列化单元。to_dict/from_dict 实现完整快照 |
| **全量快照** | Full Snapshot | 存档策略：一次性序列化所有模块状态为单个 JSON 文件 |

## 通信

| 术语 | 英文 | 定义 |
|------|------|------|
| **REST 端点** | REST Endpoint | HTTP 一次性请求：新游戏、状态查询、NPC 对话、存档 |
| **WebSocket 通道** | WebSocket | 持久双向连接 `/ws/game`：时间推进结算、事件流、NPC 行动推送 |
| **流式推送** | Streaming Push | 时段推进后逐条发送结算消息（time_advanced → event_triggered → npc_action → settlement_complete），而非一次性返回 |

## 剧情大纲与编剧

| 术语 | 英文 | 定义 |
|------|------|------|
| **剧情大纲** | Story Outline | 编剧 Agent 的核心输入。以"节拍表"替代旧 CSV 的"时刻表"，描述每段故事要讲什么，但不限定具体时间地点 |
| **故事弧线** | Story Arc | 跨越数周的叙事线（如邪教渗透线、巫女线），包含一组有序节拍 |
| **叙事节拍** | Story Beat | 弧线内的单个叙事节点。包含 `what_must_happen`（叙事内容）、`latest_by`（最晚触发时间）、`outcomes`（结果分支） |
| **台词步** | Line Step | 对话骨架中单个 NPC 的一句话/动作单元。指定 actor（谁说）、intent（意图）、must_convey（必须传达的信息）、must_avoid（必须避免的话）。区别于 Story Beat |
| **编剧 Agent** | Screenwriter Agent | 替代旧 `match_events()` 的 AI 编排器。读取大纲 + NPC 意图 → 放行/软引导/硬编排 → 生成事件。新增职责：为每个事件产出对话骨架（Dialogue Skeleton） |
| **对话骨架** | Dialogue Skeleton | 编剧 Agent 为每个事件产出的场景蓝图。包含 goal（场景目的）、tone（调性）、line_steps（台词步列表）、max_length。不含最终对白文本 |
| **场景** | Scene | 对话骨架内的一个场景单元，对应一个地点一段连续时间。由多个台词步组成 |
| **SceneFilled** | SceneFilled | NPC 根据对话骨架中自己 actor 的台词步补出的台词集合。含 line 类型（action/dialogue/thought）和对应文本 |
| **对白设计师** | Dialogue Designer | 只做文本审美的 Agent。接收拼接后的 SceneFilled，直接输出润色后完整定稿对白（同骨架 scenes + lines 结构）。不改结算数据、不加角色、不改事件走向 |
| **软引导** | Soft Guidance | 编剧 Agent 通过 `agent.remember()` 向 NPC 注入暗示记忆，NPC 下次 `think()` 自主决定是否采纳。不破坏 NPC 自主性 |
| **硬编排** | Hard Orchestration | 编剧 Agent 直接调用 `agent.dynamic.set_location()` 移动 NPC，仅在锚点事件或 `latest_by` 即将到期时使用 |
| **调性约束** | Tone Rule | 全局叙事规则（如"夜晚优先悬疑""每周至少 2 个多人社交场景"），注入编剧 Agent 的 system prompt |

## 系统模块

| 术语 | 英文 | 定义 |
|------|------|------|
| **周结算** | Week Settlement | 每周结束时的批量结算：香火度量、神力上限调整、阴德/阳德汇总 |
| **增量同步** | Delta Sync | 时段结算后只向前端返回变化部分，而非全量状态 |
