# 《命运的织线》前后端交付案

> **文件版本**: v1.0  
> **创建日期**: 2026-06-28  
> **前端**: Godot 4.4 (`godot/`)  
> **后端**: Python 3.11 + FastAPI (`src/backend/`)  
> **通信协议**: HTTP/1.1 REST + WebSocket  

---

## 目录

1. [系统架构总览](#1-系统架构总览)
2. [后端模块说明](#2-后端模块说明)
3. [前端模块说明](#3-前端模块说明)
4. [变量名映射对照表](#4-变量名映射对照表)
5. [REST API 接口](#5-rest-api-接口)
6. [WebSocket 协议](#6-websocket-协议)
7. [枚举值对齐表](#7-枚举值对齐表)
8. [数据结构完整映射](#8-数据结构完整映射)
9. [事件系统同步](#9-事件系统同步)
10. [干预系统同步](#10-干预系统同步)
11. [存档系统同步](#11-存档系统同步)
12. [联调检查清单](#12-联调检查清单)

---

## 1. 系统架构总览

### 1.1 部署架构

```
┌─────────────────────────────────────────┐
│           Godot 前端 (Win)              │
│  ┌───────────────────────────────────┐  │
│  │   BackendClient Autoload          │  │
│  │   - HTTP 请求                      │  │
│  │   - WebSocket 长连接               │  │
│  │   - 自动重连                       │  │
│  │   - Mock 降级                      │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │   BackendGameState 适配器         │  │
│  │   - 与 MockGameState 同接口       │  │
│  │   - 数据契约转换                   │  │
│  │   - 本地状态缓存                   │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
                      ↓ HTTP + WS
┌─────────────────────────────────────────┐
│      FastAPI 后端 (WSL/Linux)           │
│  http://localhost:8000                  │
│  ┌───────────────────────────────────┐  │
│  │  server/                          │  │
│  │   ├── main.py                     │  │
│  │   ├── state.py (GameSession)      │  │
│  │   └── routes/                     │  │
│  │       ├── game.py (REST)          │  │
│  │       └── ws_game.py (WS)         │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  engine/ (GameSession)            │  │
│  │   ├── time/                       │  │
│  │   ├── resource/                   │  │
│  │   ├── event/                      │  │
│  │   ├── bond/                       │  │
│  │   └── karma/                      │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### 1.2 数据流原则

| 原则 | 说明 |
|------|------|
| **后端权威** | 所有数值计算、规则判定、随机事件由后端负责，前端仅展示 |
| **前端缓存** | 后端推送最新状态，前端本地保留副本用于渲染 |
| **异步优先** | 时间推进等耗时操作通过 WebSocket 流式推送，不阻塞 UI |
| **乐观更新** | 干预执行等高频操作本地先更新，后端状态同步后修正 |
| **自动降级** | 后端不可达时自动切换 Mock 模式，前端可独立运行演示 |

---

## 2. 后端模块说明

### 2.1 目录结构

```
src/backend/
├── models/                    # 数据合同（前后端共享定义）
│   ├── npc.py                # NPC 三层结构 + 所有枚举
│   └── common.py             # ApiResponse + GameStateResponse
├── server/                    # FastAPI 服务器
│   ├── main.py               # 应用入口
│   ├── state.py              # 全局 GameSession 单例
│   ├── middleware.py         # CORS + 异常捕获
│   └── routes/
│       ├── health.py         # GET /health
│       ├── game.py           # REST 端点（11个）
│       └── ws_game.py        # WS /ws/game
├── engine/                    # 游戏逻辑引擎
│   ├── game_session.py       # 5 模块容器
│   ├── time/                 # 时间系统
│   ├── resource/             # 资源系统
│   ├── event/                # 事件系统
│   ├── bond/                 # 缘线系统
│   └── karma/                # 业线系统
├── ai/npc_agent/             # NPC AI 代理（LLM驱动）
├── storage/                   # 存档持久化
└── tests/                     # 单元测试（308个）
```

### 2.2 核心类

| 后端类 | 文件 | 职责 |
|--------|------|------|
| `GameSession` | `engine/game_session.py` | 游戏状态总容器，5 子模块引用 |
| `TimeState` | `engine/time/` | 天/时段/周/周阶段管理 |
| `ResourceState` | `engine/resource/` | 香火/神力/阴阳德管理 |
| `AgentManager` | `ai/npc_agent/manager.py` | 管理所有 NPC Agent |
| `NpcAgent` | `ai/npc_agent/agent.py` | 单个 NPC AI（static/dynamic/memory 三层） |
| `BondManager` | `engine/bond/` | NPC 间关系数值管理 |
| `KarmaManager` | `engine/karma/` | 业线节点进度管理 |
| `EventExecutor` | `engine/event/` | 事件触发 + 结算 + 命运硬币 |

---

## 3. 前端模块说明

### 3.1 目录结构

```
godot/
├── scripts/
│   ├── app/
│   │   ├── backend_client.gd        # BackendClient Autoload
│   │   ├── settings_service.gd      # 设置服务
│   │   └── tutorial_state.gd        # 教程状态
│   └── adapters/
│       ├── mock_game_state.gd       # 本地 Mock 适配器
│       └── backend_game_state.gd    # 后端适配器（与Mock同接口）
└── scenes/
    ├── main/Main.tscn               # 主场景（根节点）
    └── ui/                          # 全部 UI 组件
```

### 3.2 核心类

| 前端类 | 文件 | 职责 |
|--------|------|------|
| `BackendClient` | `scripts/app/backend_client.gd` | Autoload，封装 HTTP + WS 通信 |
| `BackendGameState` | `scripts/adapters/backend_game_state.gd` | 状态适配器，API 与 Mock 完全一致 |
| `MockGameState` | `scripts/adapters/mock_game_state.gd` | 本地 Mock 数据适配器 |

### 3.3 状态切换

在 `main.gd` 中：
```gdscript
## true = 使用真实后端，false = 使用本地 Mock
const USE_BACKEND := true
```

---

## 4. 变量名映射对照表

### 4.1 核心资源变量

| 说明 | 后端（Python） | 前端（GDScript） | 类型 | 值域 |
|------|---------------|-------------------|------|------|
| 当前天数 | `GameSession.time.day` | `_state_cache["day"]` | int | 1~60 |
| 当前时段 | `GameSession.time.slot` | `_state_cache["time_slot"]` | enum | 见枚举表 |
| 当前周 | `GameSession.time.week` | `_state_cache["week"]` | int | 1~9 |
| 香火 | `GameSession.resource.incense` | `_state_cache["incense"]` | int | ≥0 |
| 神力（当前） | `GameSession.resource.divine_power` | `_state_cache["divine_power"]` | int | ≥0 |
| 神力（上限） | `GameSession.resource.divine_power_max` | `_state_cache["divine_power_max"]` | int | 固定10 |
| 阴德 | `GameSession.resource.yin_de` | `_state_cache["yin_de"]` | int | 0~100 |
| 阳德 | `GameSession.resource.yang_de` | `_state_cache["yang_de"]` | int | 0~100 |

### 4.2 NPC 静态层变量

| 说明 | 后端路径 | 前端路径 | 类型 |
|------|---------|---------|------|
| NPC ID | `NpcAgent.static.id` | `npc["npc_id"]` | str |
| 姓名 | `NpcAgent.static.name` | `npc["name"]` / `npc["display_name"]` | str |
| 年龄 | `NpcAgent.static.age` | `npc["age"]` | int |
| 职业 | `NpcAgent.static.occupation` | `npc["occupation"]` | str |
| Agent 级别 | `NpcAgent.static.tier` | `npc["tier"]` | AgentTier |
| 背景故事 | `NpcAgent.static.background` | `npc["background"]` | str |
| 核心愿望 | `NpcAgent.static.core_wish` | `npc["core_wish"]` | str |
| 五维性格 | `NpcAgent.static.personality` | `npc["personality"]` | dict |
| 四围属性 | `NpcAgent.static.attributes` | `npc["attributes"]` | dict |

### 4.3 NPC 动态层变量

| 说明 | 后端路径 | 前端路径 | 类型 |
|------|---------|---------|------|
| 当前位置 | `NpcAgent.dynamic.location` | `npc["location"]` | Location |
| 幸福度 | `NpcAgent.dynamic.happiness` | `npc["happiness"]` | int (0~100) |
| 精力值 | `NpcAgent.dynamic.energy` | `npc["energy"]` | int (0~100) |
| 当前情绪 | `NpcAgent.dynamic.emotion` | `npc["emotion"]` | Emotion |
| 当前目标 | `NpcAgent.dynamic.current_goal` | `npc["current_goal"]` | str |
| 主业线进度 | `NpcAgent.dynamic.karma_main_progress` | `npc["karma_main_progress"]` | float (0~1) |
| 支线进度 | `NpcAgent.dynamic.karma_side_progress` | `npc["karma_side_progress"]` | dict |

### 4.4 缘线变量

| 说明 | 后端 | 前端 | 类型 |
|------|------|------|------|
| 两人缘分值 | `BondManager.get_bond(a, b)` | - | int |
| 缘线类型 | `BondManager.get_type(a, b)` | - | str |
| 所有缘线字典 | `BondManager.to_dict()` | - | dict |

### 4.5 业线变量

| 说明 | 后端 | 前端 | 类型 |
|------|------|------|------|
| NPC 业线进度 | `KarmaManager.get_progress(npc_id)` | - | float |
| 当前节点 | `KarmaManager.get_current_node(npc_id)` | - | str |
| 已完成节点 | `KarmaManager.get_completed_nodes(npc_id)` | - | list[str] |

---

## 5. REST API 接口

### 5.1 基础信息

| 项 | 值 |
|----|----|
| Base URL | `http://localhost:8000` |
| Content-Type | `application/json` |
| 统一响应格式 | `{ success: bool, data: any, error: str }` |

### 5.2 端点列表

| 方法 | 路径 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| GET | `/health` | 健康检查 | - | `{"status": "ok", "timestamp": float}` |
| POST | `/new-game` | 创建新游戏 | - | `GameStateResponse` |
| GET | `/state` | 查询完整状态 | - | `GameStateResponse` |
| POST | `/chat/{npc_id}` | NPC 对话 | `{ "message": str, "day": int }` | `{ "npc_id", "npc_name", "response" }` |
| POST | `/save/{slot}` | 保存游戏 | - | `{ "slot": int, "day": int }` |
| POST | `/load/{slot}` | 加载游戏 | - | `GameStateResponse` |
| GET | `/saves` | 列出存档 | - | `[{ slot, day, timestamp, exists }]` |
| DELETE | `/saves/{slot}` | 删除存档 | - | `{ "slot": int, "deleted": true }` |

### 5.3 GameStateResponse 结构（前后端通用）

```python
{
    "day": int,               # 1~60
    "slot": str,              # "morning" / "noon" / "night"
    "incense": int,           # 香火
    "divine_power": int,      # 神力当前
    "yin_de": int,            # 阴德
    "yang_de": int,           # 阳德
    "npcs": [NpcSnapshot],    # 所有 NPC 快照
    "npc_total": int          # NPC 总数
}
```

### 5.4 NpcSnapshot 结构

```python
{
    "static": {               # 静态层（不变）
        "id": str,
        "name": str,
        "age": int,
        "occupation": str,
        "tier": str,          # "S" / "A" / "B" / "C"
        "background": str,
        "attributes": {       # 四围 1~10
            "mind": int,
            "faith": int,
            "physique": int,
            "charm": int
        },
        "personality": {      # 五维 0~1
            "kindness": float,
            "aggression": float,
            "sensibility": float,
            "rationality": float,
            "curiosity": float
        },
        "core_wish": str
    },
    "dynamic": {              # 动态层（每时段变化）
        "location": str,
        "happiness": int,
        "energy": int,
        "emotion": str,
        "current_goal": str,
        "karma_main_progress": float,
        "karma_side_progress": {}
    },
    "memory_summary": {}      # 记忆摘要（MVP 暂空）
}
```

---

## 6. WebSocket 协议

### 6.1 连接信息

| 项 | 值 |
|----|----|
| 端点 | `ws://localhost:8000/ws/game` |
| 协议 | WebSocket (RFC 6455) |
| 消息格式 | JSON |
| 重连 | 断开后自动重试（前端 BackendClient 实现） |

### 6.2 通用消息格式

```typescript
// 所有消息的通用外壳
{
    "type": string,          // 消息类型
    "payload": object,       // 业务数据
    "timestamp": number,     // unix 时间戳
    "request_id": string     // 请求/响应关联ID
}
```

### 6.3 客户端 → 服务端消息

| type | 说明 | payload |
|------|------|---------|
| `new_game` | 创建新游戏 | `{}` |
| `advance_time` | 推进一个时段 | `{}` |
| `get_state` | 查询状态 | `{}` |
| `chat` | NPC 对话 | `{ npc_id: str, message: str, day: int }` |
| `apply_intervention` | 执行干预 | `{ event_id: str, intervention_id: str }` |
| `save_game` | 保存 | `{ slot: int }` |
| `load_game` | 加载 | `{ slot: int }` |

### 6.4 服务端 → 客户端消息

| type | 触发时机 | payload | 对应前端信号 |
|------|----------|---------|-------------|
| `time_advanced` | 时间刚推进，未结算前 | `{ day, slot, week, is_new_day, is_new_week, phase_name }` | `Backend.time_advanced` |
| `event_triggered` | 每个事件触发时发送（可多次） | `{ event_id, event_name, location_id, risk_level, description, outcome, resource_delta, participants, available_interventions }` | `Backend.event_triggered` |
| `npc_action` | 每个 S/A 级 NPC 决策后 | `{ npc_id, name, action: str }` | `Backend.npc_action` |
| `settlement_complete` | 本时段所有结算完成 | `{ day, slot, incense, divine_power, divine_power_max, yin_de, yang_de, npcs: NpcSnapshot[] }` | `Backend.settlement_complete` |
| `game_created` | 新游戏创建完成 | `GameStateResponse` | `Backend.new_game_completed` |
| `game_state` | 状态查询响应 | `GameStateResponse` | `Backend.state_completed` |
| `game_saved` | 保存完成 | `{ slot, day }` | `Backend.save_completed` |
| `game_loaded` | 加载完成 | `GameStateResponse` | `Backend.load_completed` |
| `npc_response` | LLM 对话响应 | `{ npc_id, npc_name, response: str }` | `Backend.npc_response` |
| `error` | 任何错误 | `{ message: str }` | `Backend.error` |

### 6.5 时间推进完整时序

```
1. 前端 send_advance_time()
    ↓
2. 后端 TimeState.advance()
    ↓
3. 后端推送 time_advanced
    ↓ Frontend: 更新天/时段，不刷新UI
4. 循环：每个触发的事件推送 event_triggered
    ↓ Frontend: 累积到 _pending_events，写入历史
5. 循环：每个 S/A 级 NPC think() 后推送 npc_action
    ↓ Frontend: 可在右下角显示气泡提示
6. 最终结算完成，推送 settlement_complete
    ↓ Frontend: 更新所有资源/NPC，emit state_changed → UI 全刷新
```

---

## 7. 枚举值对齐表

### 7.1 时段枚举 Slot

| 语义 | 后端值 | 前端值 | 前端中文 |
|------|--------|--------|---------|
| 早晨 | `"morning"` | `"Morning"` | 早晨 |
| 下午 | `"noon"` | `"Afternoon"` | 下午 |
| 夜晚 | `"night"` | `"Night"` | 夜晚 |

> ⚠️ **注意**：后端是 `noon`，前端是 `Afternoon`，适配器需转换。

### 7.2 地点枚举 Location

| 后端值 | 前端值 | 中文名 |
|--------|--------|-------|
| `"temple"` | `"temple"` | 土地庙 |
| `"plaza"` | `"plaza"` | 广场 |
| `"beach"` | `"beach"` | 沙滩 |
| `"school"` | `"school"` | 学校 |
| `"clinic"` | `"clinic"` | 诊所 |
| `"shopping_street"` | `"shopping_street"` | 商业街 |
| `"bookstore"` | `"bookstore"` | 书店 |
| `"cafe"` | `"cafe"` / `"coffee_shop"` | 咖啡馆 |
| `"police_station"` | `"police_station"` | 警局 |
| `"mountain_forest"` | `"mountain_forest"` | 山林 |
| `"port"` | `"port"` | 港口 |
| `"residence"` | `"residence"` | 住所 |

> ⚠️ **注意**：前端 Mock 有 `wine_bar` / `seafood_shop`，后端暂不支持。

### 7.3 情绪枚举 Emotion

| 后端值 | 说明 |
|--------|------|
| `"happy"` | 开心 |
| `"sad"` | 难过 |
| `"angry"` | 生气 |
| `"fearful"` | 害怕 |
| `"calm"` | 平静 |
| `"excited"` | 兴奋 |
| `"anxious"` | 焦虑 |
| `"neutral"` | 中立 |

### 7.4 AgentTier 枚举

| 后端值 | 说明 |
|--------|------|
| `"S"` | 完整 LLM 驱动，完整记忆系统 |
| `"A"` | 半完整，关键事件记忆 + LLM |
| `"B"` | 规则驱动，状态机 |
| `"C"` | 轻量，固定反应 |

### 7.5 事件风险等级 RiskLevel

| 后端值 | 说明 |
|--------|------|
| `"Low"` | 无负面影响 |
| `"Medium"` | 轻微负面，可逆转 |
| `"High"` | 严重负面，需干预 |
| `"Fatal"` | 致命，可能直接坏结局 |

---

## 8. 数据结构完整映射

### 8.1 前后端字段转换规则（BackendGameState）

在 `backend_game_state.gd::_normalize_state` 中：

```gdscript
func _normalize_state(raw: Dictionary) -> Dictionary:
    var result = raw.duplicate()

    # 时段名称转换
    var slot = String(raw.get("slot", "morning"))
    if slot == "noon":
        result["time_slot"] = "Afternoon"
    elif slot == "night":
        result["time_slot"] = "Night"
    else:
        result["time_slot"] = "Morning"

    # 神力字段补全
    if result.has("divine_power") and not result.has("divine_power_max"):
        result["divine_power_max"] = 10

    # 确保 day 存在
    if not result.has("day"):
        result["day"] = raw.get("day", 1)

    return result
```

### 8.2 NPC 扁平化转换

```gdscript
func _normalize_npcs(raw_list: Array) -> Array:
    var result: Array = []
    for raw in raw_list:
        var npc = raw.duplicate()
        var static = npc.get("static", {}) as Dictionary
        var dynamic = npc.get("dynamic", {}) as Dictionary

        # 静态层展开
        npc["npc_id"] = static.get("id", "")
        npc["name"] = static.get("name", "")
        npc["display_name"] = static.get("name", "")  # 与 Mock 对齐
        npc["occupation"] = static.get("occupation", "")
        npc["tier"] = static.get("tier", "C")
        npc["age"] = static.get("age", 30)
        npc["background"] = static.get("background", "")
        npc["core_wish"] = static.get("core_wish", "")
        npc["personality"] = static.get("personality", {})
        npc["attributes"] = static.get("attributes", {})

        # 动态层展开
        npc["location"] = dynamic.get("location", "plaza")
        npc["happiness"] = dynamic.get("happiness", 50)
        npc["energy"] = dynamic.get("energy", 100)
        npc["emotion"] = dynamic.get("emotion", "neutral")
        npc["current_goal"] = dynamic.get("current_goal", "")
        npc["karma_main_progress"] = dynamic.get("karma_main_progress", 0.0)
        npc["karma_side_progress"] = dynamic.get("karma_side_progress", {})

        result.append(npc)
    return result
```

---

## 9. 事件系统同步

### 9.1 事件触发流程

```
后端 advance_time()
    ↓
TimeState.advance()
    ↓
EventMatcher.match_events()  # 按时间/地点/NPC匹配
    ↓
循环每个匹配的事件：
    EventExecutor.execute()  # 执行事件效果
    推送 event_triggered 消息 → 前端接收
    ↓
所有事件执行完毕
    ↓
结算所有资源/NPC变化
    ↓
推送 settlement_complete 消息 → 前端更新UI
```

### 9.2 事件数据映射

| 字段 | 后端来源 | 前端路径 |
|------|---------|---------|
| 事件ID | `EventTemplate.id` | `event["event_id"]` |
| 事件名称 | `EventTemplate.name` | `event["event_name"]` |
| 地点ID | `EventTemplate.location` | `event["location_id"]` |
| 风险等级 | `EventTemplate.risk_level` | `event["risk_level"]` |
| 参与NPC | `EventTemplate.participants` | `event["participants"]` |
| 可用干预 | 运行时计算 | `event["available_interventions"]` |

### 9.3 事件历史同步

前端 `BackendGameState._on_event_triggered`：
```gdscript
func _on_event_triggered(data: Dictionary) -> void:
    # 写入事件历史
    var day = int(_state_cache.get("day", 1))
    var slot_id = String(_state_cache.get("time_slot", "Morning"))
    event_history.append({
        "day": day,
        "time_slot": slot_id,
        "time_slot_label": get_time_slot_label(slot_id),
        "location_id": String(data.get("location_id", "")),
        "location_label": get_location_label(String(data.get("location_id", ""))),
        "event_id": String(data.get("event_id", "")),
        "event_name": String(data.get("event_name", "未知事件")),
        "summary": String(data.get("description", "")),
        "risk_level": String(data.get("risk_level", "Low")),
        "participant_names": data.get("participants", []),
    })
    # 累积到待显示事件列表
    _events.append(data)
```

---

## 10. 干预系统同步

### 10.1 干预执行流程

```
用户点击事件详情中的「干预」
    ↓
前端检查神力是否足够
    ↓
Backend.send_intervention(event_id, intervention_id)
    ↓ [WebSocket]
后端 EventExecutor.apply_intervention()
    ↓
执行效果：修改资源 / 缘线 / 业线 / NPC状态
    ↓
后端推送 settlement_complete（含更新后数值）
    ↓
前端更新本地缓存
```

### 10.2 前端乐观更新

为避免 UI 卡顿，前端在发送干预请求时先本地扣减神力：
```gdscript
func apply_intervention(event_id: String, intervention_id: String) -> Dictionary:
    var cost = 2  # 可由后端动态配置
    var current_power = int(_state_cache.get("divine_power", 0))
    if current_power < cost:
        return {"success": false, "summary": "神力不足"}

    # 乐观更新：先扣减，后端会在结算时纠正
    _state_cache["divine_power"] = current_power - cost
    state_changed.emit()

    if not Backend.use_mock_fallback:
        Backend.send_intervention(event_id, intervention_id)

    return {"success": true, "cost": cost, ...}
```

---

## 11. 存档系统同步

### 11.1 存档数据结构

后端 `JsonStorage` 保存的完整结构：
```python
{
    "version": "1.0",
    "slot": int,
    "saved_at": float,  # timestamp
    "day": int,
    "time": TimeState.to_dict(),
    "resource": ResourceState.to_dict(),
    "agents": AgentManager.to_dict(),
    "bonds": BondManager.to_dict(),
    "karma": KarmaManager.to_dict(),
}
```

### 11.2 存档流程

```gdscript
# 前端调用
BackendGameState.save_game(1)
    ↓
Backend.send_save(1)
    ↓ [WS]
后端 storage.save() → 写入 user://saves/slot_1.json
    ↓
推送 game_saved { slot, day }
    ↓
前端显示 Toast："已保存到槽位 1"
```

### 11.3 读档流程

```gdscript
BackendGameState.load_game(1)
    ↓
Backend.send_load(1)
    ↓ [WS]
后端 storage.load() → GameSession.from_dict()
    ↓
推送 game_loaded（完整 GameStateResponse）
    ↓
前端 _on_load_completed() 清空所有缓存，重新初始化
    ↓
state_changed.emit() → UI 全刷新
```

---

## 12. 联调检查清单

### 12.1 启动与连接

- [ ] 后端启动 `uvicorn src.backend.server.main:app --host 0.0.0.0 --port 8000` 正常
- [ ] 访问 `http://localhost:8000/docs` 可打开 Swagger UI
- [ ] Godot 设置 `USE_BACKEND=true`
- [ ] Godot 运行，BackendClient 健康检查通过，未降级到 Mock
- [ ] WebSocket 连接建立，无报错

### 12.2 新游戏流程

- [ ] 点击「新游戏」，后端 /new-game 返回 200
- [ ] 天/时段/资源数值正确显示
- [ ] NPC 列表正确渲染，头像/名称/位置匹配
- [ ] 地点地图上 NPC 分布正确

### 12.3 时间推进流程

- [ ] 点击「推进时间」按钮
- [ ] 收到 time_advanced 消息，天/时段正确变化
- [ ] 收到 0~N 个 event_triggered 消息
- [ ] 事件列表正确显示新事件
- [ ] 收到 settlement_complete 消息
- [ ] 资源数值正确更新
- [ ] NPC 位置/状态正确更新

### 12.4 事件与干预

- [ ] 点击事件，详情弹窗正确
- [ ] 风险等级 High/Fatal 事件有红色警示
- [ ] 点击干预，神力正确扣减
- [ ] 干预效果在下次结算中体现

### 12.5 NPC 对话

- [ ] 点击 NPC 弹出对话窗口
- [ ] 发送消息，后端 LLM 响应（S/A 级）
- [ ] 对话响应正确显示在 UI

### 12.6 存档读档

- [ ] 存档成功，文件生成
- [ ] 推进时间后读档，状态正确回滚
- [ ] 存档列表正确显示槽位和天数

### 12.7 异常处理

- [ ] 后端断开，前端自动降级到 Mock 模式
- [ ] 网络恢复后可正常重连（需手动实现重连逻辑）
- [ ] 错误消息正确 Toast 提示，不崩溃

---

## 附录：文件对照表

| 功能 | 后端文件 | 前端文件 |
|------|---------|---------|
| NPC 数据模型 | `src/backend/models/npc.py` | `scripts/adapters/backend_game_state.gd` |
| 游戏状态响应 | `src/backend/models/common.py` | 同上 |
| REST API | `src/backend/server/routes/game.py` | `scripts/app/backend_client.gd` |
| WebSocket API | `src/backend/server/routes/ws_game.py` | 同上 |
| 时间推进 | `src/backend/engine/time/` | `scripts/adapters/backend_game_state.gd` |
| 资源管理 | `src/backend/engine/resource/` | 同上 |
| 事件系统 | `src/backend/engine/event/` | 同上 |
| 缘线系统 | `src/backend/engine/bond/` | 待接入 |
| 业线系统 | `src/backend/engine/karma/` | 待接入 |
| NPC AI | `src/backend/ai/npc_agent/` | 待接入 |
| 存档系统 | `src/backend/storage/` | 同上 |

---

**交付说明**：本文档随项目迭代更新，每次接口变更请同步更新对应章节。
