# 《命运的织线》(Threads of Fate) — AI 原生叙事的神明模拟游戏

**角色**:核心策划 / AI Agent 系统设计负责人
**周期**:2026.05 – 至今
**技术栈**:Python (FastAPI, asyncio) · Godot 4.4 · Anthropic Claude API · llama.cpp 本地推理 · WebSocket · pytest

## 项目简介

一款以"土地公"为主角的神明模拟游戏。玩家不能直接控制人类意志,只能在 60 个游戏日内通过托梦、赐福、遮蔽信息等方式微调 14 位 NPC 的命运走向。**每个 NPC 由独立 LLM Agent 驱动**,拥有自己的性格、记忆、目标;剧情由编剧 Agent 根据"叙事节拍表"实时编排,而非走硬编码分支。目标是让"因果与涌现"成为核心乐趣。

## 核心挑战 & 解决方案

**挑战 1:LLM 输出的不可预测性 vs 游戏规则的确定性**
定义了 7 条"AI 生成边界"原则(规则优先 / AI 不写状态 / 可降级 / 不剧透 / 不扩权 / 可审核 / 内容克制),形成 **"规则决定结果,AI 只包装表达"** 的架构准则。LLM 决策必须落到预定义事件模板 + 数值 delta,不能直接写存档、资源、结局。

**挑战 2:涌现叙事的自由度 vs 主线节奏的可控性**
放弃传统"Week × Day × Slot × Location"硬编码事件表,自研 **StoryOutline 节拍系统**——只描述"这段故事要表达什么"而非"何时何地必发生"。编剧 Agent 根据 NPC 实时状态与意图,在"放行 / 软引导(记忆注入) / 硬编排"三档中做出决策。`must_happen` + `latest_by` 双兜底,确保关键节拍不丢失。

**挑战 3:NPC 记忆无限累积 vs LLM 上下文窗口有限**
设计 **三层记忆架构**:静态属性(不变) / 动态状态(每时段变) / 记忆池(持续累积)。记忆池分事件链(≤50)、关键记忆(Top 30 按重要性排序)、情感印象(好感/信任/标签)、溢出自动摘要四层,保证 System Prompt 稳定在 4K tokens 以内。

**挑战 4:多 Agent 并发调用的稳定性与成本**
搭建 **多层 LLM 降级链**:Anthropic API → 本地 llama.cpp 负载均衡(多端点轮询 + per-endpoint Semaphore + 故障转移 + 指数退避)→ 规则模板兜底。JSON 输出用严格解析 → 正则提取 → 完全失败回退的 **三层容错**,确保任何一层挂掉游戏仍可运行。

**挑战 5:敏感剧情(邪教、家庭冲突、悲剧结局)的表达**
制定内容克制策略——通过暗示、后果、氛围与角色反应表达黑暗主题,拒绝直接描写。所有 AI 提示词模板经过内容边界评审。

## 项目成果

- **多 Agent 系统落地**:NPC Agent(14 个实例)+ 编剧 Agent + 叙事润色 Agent 三类 Agent 协作,后端约 **12,000 行 Python 代码**,72 个模块
- **叙事引擎**:W1–W2 大纲已产出 3 条弧线 12 个节拍,支撑 60 天 3 时段的时序推进
- **稳定性**:9 个 pytest 测试套件(story 模块单独 34 个用例),LLM 降级链在无网络/无 GPU 环境下也可 headless 跑通
- **文档产出**:10 份系统策划案(时间/资源/干预/NPC/缘线业线/事件/结局/存档/AI 边界/剧情大纲)、14 位 NPC 详设、MVP 竖切脚本
- **前后端联调**:Godot 竖切默认联网,自动降级 Mock;WebSocket 通信,支持增量状态同步(delta patch)。

## 关键个人贡献

- 定义了 AI Agent 与游戏规则的交互协议与边界
- 主导 StoryOutline 数据结构从 CSV 时刻表演进到节拍表
- 设计三层 NPC 记忆架构与 Prompt 模板策略
- 撰写全部系统策划文档与内容克制规范

---

# 附录 A:多 Agent 稳定性架构详解(挑战 4 深挖)

## A.1 为什么这是核心问题

一个时段的 LLM 调用规模:

- 14 个 NPC Agent 各自 `think()` → **最多 14 次调用**
- 1 个编剧 Agent 做叙事编排 → **1 次调用**
- 1 个叙事润色 Agent 把事件写成自然语言 → **N 次调用**

一天 3 时段、一局 60 天,理论最多 **3000–4000 次 LLM 请求**。带来三个现实约束:

| 问题 | 结果 |
|---|---|
| **API 成本**:Claude 3.5 Sonnet ~$3/M tokens,几千次调用积累开销可观 | 玩家不愿付、发行难 |
| **服务稳定性**:公网 API 会 429/503/超时;本地 GPU 显存打满会崩 | 一次崩塌 = 整局卡死 |
| **LLM 幻觉**:让 LLM 返回 JSON,它偶尔会返回"好的,以下是 JSON:\`\`\`json{...}\`\`\`"或多加解释文字 | `json.loads` 直接抛异常 |

于是做了 **两条独立容错链**:一条抗"服务不可用",一条抗"输出格式不对"。

## A.2 第一条链:LLM 调用降级链

```
┌────────────────────────────────────────────────────────────┐
│ Tier 1: Anthropic Claude API (质量最好,成本最高)            │
│  → 生产环境首选,适合关键叙事节拍                            │
└────────────────────────────────────────────────────────────┘
             │ 网络挂 / 超预算 / 用户没配 API key
             ▼
┌────────────────────────────────────────────────────────────┐
│ Tier 2: 本地 llama.cpp 负载均衡 (免费,自己电脑跑)           │
│  ├─ 多端点轮询:如 :8081 / :8082 / :8083 三个 llama 进程   │
│  ├─ per-endpoint Semaphore:每端点只允许 2 个并发           │
│  ├─ 故障转移:一个端点 500,自动换下一个                    │
│  └─ 指数退避:429/503 时等 1.5 / 2.25 / 3.4 秒再重试        │
└────────────────────────────────────────────────────────────┘
             │ 全部 llama.cpp 都挂
             ▼
┌────────────────────────────────────────────────────────────┐
│ Tier 3: 规则模板兜底 (100% 稳定,文本单调)                  │
│  → 直接拿预先写好的模板句子填入变量,不走 AI                │
└────────────────────────────────────────────────────────────┘
```

### 四个关键技术点

**① 多端点轮询 (Round-Robin)**
llama.cpp 单进程占一个端口。启动 3 个进程 = 3 个"工人"挂载同一模型。请求依次分给 :8081 → :8082 → :8083,吞吐量约提升 3 倍。
代码:`load_balanced.py::_next_index()`,`self._counter % len(endpoints)`。

**② per-endpoint Semaphore(每端点并发限制)**
`Semaphore(2)` 意即"这个端点同时最多 2 个人进来"。**llama.cpp 单进程同时收 5 个请求会显存 OOM 崩溃**。Semaphore 相当于门口保安,第 3 个请求在门外排队。
代码:`load_balanced.py::__init__()` `asyncio.Semaphore(per_endpoint_concurrency)`。

**③ 故障转移 (Failover)**
`tried: set[int]` 记录已试过的端点。端点 A 超时/500 后加入 `tried`,轮询跳过,直接换 B。**不在同一坏端点反复撞墙**。

**④ 指数退避 (Exponential Backoff)**
遇 429 (太快了) 或 503 (服务器忙),不立刻重试,等 `1.5^1=1.5s` → `1.5^2=2.25s` → `1.5^3=3.4s`。给服务器恢复时间。
代码:`load_balanced.py` `wait = self._retry_backoff_base ** (retry + 1)`。

### 整体效果

| 场景 | 结果 |
|---|---|
| 玩家有网 + 有 API key | 走 Claude API,最好叙事质量 |
| 玩家没网、有本地 GPU | 走 llama.cpp,质量降一点但免费 |
| 玩家没网、没 GPU、没 key | 走规则模板,文本单调但**游戏能通关** |
| Claude 挂 5 分钟(公网故障) | 自动切 llama.cpp,玩家几乎无感 |

**关键**:游戏永远能跑,LLM 是"锦上添花",不是"命门"。

## A.3 第二条链:JSON 输出三层容错

编剧 Agent 需 LLM 返回结构化决策:
```json
{"decision": "soft_guidance", "target_beat": "b_w1_first_meeting", "hint": "..."}
```

但 LLM 常返回:
```
好的,根据当前 NPC 状态,我的判断如下:
```json
{"decision": "soft_guidance", ...}
```
希望对你有帮助!
```

三层容错顺序尝试:

```
┌───────────────────────────────────────────────┐
│ Tier 1: 直接 json.loads(text)                  │
│  → 干净 JSON,一次就过 (~85% 情况)              │
└───────────────────────────────────────────────┘
              │ JSONDecodeError
              ▼
┌───────────────────────────────────────────────┐
│ Tier 2: 正则抓最外层 {...}                     │
│  regex: r'\{[\s\S]*\}'                        │
│  → 把 "好的:{...}希望有帮助" 里的 {} 抠出     │
│  → ~10% 情况被这层救回                         │
└───────────────────────────────────────────────┘
              │ 还是 JSONDecodeError
              ▼
┌───────────────────────────────────────────────┐
│ Tier 3: 正则抓 ```json ... ``` 代码块           │
│  regex: r'```(?:json)?\s*([\s\S]*?)```'       │
│  → 处理 LLM 爱包 Markdown 代码块的情况          │
│  → ~4% 情况被这层救回                          │
└───────────────────────────────────────────────┘
              │ 三层都失败 (~1%)
              ▼
        返回 None
        → 上层退回 CSV match_events()
        → 走旧版硬编码事件表
```

代码在 `screenwriter.py::_parse_llm_output()`。

三层加起来把成功率从 85% 拉到 **99%**,代价只是几行正则。

---

# 附录 B:面试速答卡

## B.1 30 秒电梯陈述

> "我在做一款叫《命运的织线》的神明模拟游戏。玩家扮演土地公,60 天内通过间接干预(托梦、赐福、遮蔽信息)推动 14 位 NPC 的命运。每个 NPC 都是独立的 LLM Agent,拥有自己的性格、记忆、目标;剧情由一个'编剧 Agent'根据叙事节拍表实时编排,而不是硬编码分支。我主要负责 AI Agent 系统的设计与边界定义。"

## B.2 项目定位类

**Q: 为什么用 LLM 做 NPC 而不是行为树?**
A: 行为树的 NPC 反应可预测,玩家干预两次就摸清模式了。LLM 驱动的 NPC 有性格和记忆,同样的干预在不同时刻给出不同反应,涌现感强得多。但——只让 LLM 做"反应",不让它决定游戏结局。

**Q: 这不就是一堆 AI 聊天机器人凑一起吗?和 AI 女友类产品有啥不同?**
A: 三个本质区别:
1. **NPC 之间会互动**:AI 之间的关系形成叙事图谱,不是各自和玩家对话
2. **玩家不能直接对话**:强制"神明视角",只能间接干预,倒逼玩家用系统思维而非对话套路
3. **规则决定结果**:LLM 不改数值状态,游戏平衡由本地规则守住

**Q: LLM 幻觉会破坏游戏平衡吗?**
A: 7 条 AI 生成边界原则,核心是"**规则决定结果,AI 只包装表达**"。LLM 只输出决策标签(如"去咖啡厅"),不能直接改数值。数值变化全部由本地规则应用。

**Q: 你觉得这游戏能商业化吗?**
A: MVP 阶段先验证核心循环——只做 W1-W2 的 12 节拍。如果玩家试玩报告说"NPC 感觉像真人"就继续扩,否则回退到更传统的分支叙事。已经准备了 AI 边界文档控制内容风险,商业化门槛主要在 LLM 成本能不能压到合理区间。

## B.3 系统架构类

**Q: 你们的 AI Agent 系统有几类?**
A: 三类:
- **NPC Agent**(14 个实例):独立记忆、独立对话历史,决定"这个时段我做什么"
- **编剧 Agent**(1 个):协调所有 NPC 行为形成叙事节拍,输出软引导/硬编排指令
- **Narrator Agent**(1 个):把结构化事件结果润色成自然语言

**Q: NPC 记忆系统怎么设计的?**
A: 三层架构:
- **静态属性**(不变):姓名、年龄、性格模型、核心愿望
- **动态状态**(每时段变):位置、幸福度、精力、当前目标、业线进度
- **记忆池**(累积):事件链(≤50 条) + 关键记忆(Top 30 按重要性) + 情感印象(好感/信任/标签) + 溢出自动摘要

保证 System Prompt 稳定在 **4K tokens 以内**。

**Q: 编剧 Agent 怎么控制主线不跑偏?**
A: 关键是数据结构升级。旧版 CSV 硬编码"W1 Day3 咖啡厅 事件 A",新版 **StoryOutline** 只描述"这段故事要表达什么"。编剧 Agent 每时段做三档决策:
- **放行**:NPC 想干嘛干嘛
- **软引导**:把节拍提示注入 NPC 记忆,让它"自愿"往那走
- **硬编排**:关键节点强制触发事件

加上 `must_happen` + `latest_by` 双兜底,保证叙事骨架不散。

**Q: 为什么要区分软引导和硬编排?**
A: 全软引导 → LLM 太自由,主线经常错过;全硬编排 → NPC 变木偶,涌现感全无。软引导覆盖 80% 场景,硬编排保 20% 关键节点,这个比例是玩下来调出来的。

## B.4 工程 / 技术类

**Q: 一局要调多少次 LLM?**
A: 一时段 15-20 次(14 NPC + 编剧 + N 润色),一天 3 时段,一局 60 天,理论最多 3000-4000 次。所以稳定性和成本是核心问题。

**Q: 怎么控制成本?**
A: 三层降级链:Claude API → 本地 llama.cpp 集群 → 规则模板。生产用 Claude 保质量,本地开发和 CI 用 llama.cpp,单测和无网环境走规则兜底。玩家也可以选自己的 API key。

**Q: 本地 llama.cpp 集群怎么配的?**
A: 多进程各占一个端口,轮询分发。per-endpoint Semaphore 限单机并发防 OOM。故障转移 + 指数退避处理临时故障。用 `httpx.AsyncClient` + 连接池复用降低握手开销。

**Q: 为什么用 Semaphore 而不是 Queue?**
A: Semaphore 更轻量,无阻塞语义,直接契合 `async with` 语法。Queue 适合按顺序处理的场景,LLM 请求本身无序。

**Q: 为什么指数退避 base 是 1.5 不是 2?**
A: 2 增长太快,3 次重试就 8 秒了玩家等不了。1.5 在"礼貌"和"响应快"之间平衡。

**Q: LLM 输出 JSON 不稳定怎么办?**
A: 三层解析:直接 `json.loads` → 正则抓 `{...}` → 正则抓 markdown 代码块。实测把成功率从 85% 拉到 99%。都失败就退回旧版 CSV 事件匹配,游戏不中断。

**Q: 为什么不用 function calling / structured output?**
A: 三个理由:
1. Anthropic 支持 tool use,但本地 llama.cpp 支持不完美,统一走 JSON 解析降低复杂度
2. Grammar-constrained decoding (GBNF/Outlines) 会拖慢生成速度
3. 现有正则兜底成本极低,是刻意的低配选择

**Q: 前后端怎么通信?为什么是 WebSocket?**
A: WebSocket + JSON。游戏节奏是"时段推进",服务器算完一时段要主动推给客户端,HTTP 轮询浪费;而且要支持 delta patch(只推变化字段,不推全量状态),WebSocket 天然合适。

## B.5 内容安全类

**Q: 剧情里有邪教、家庭矛盾,LLM 会写不合适的东西吗?**
A: 三重防护:
1. **System Prompt 明文禁止**:列出禁写清单
2. **AI 只润色不写状态**:即使写偏也不影响游戏推进
3. **"内容克制"原则**:用暗示、后果、氛围表达黑暗主题,不做露骨描写

**Q: 玩家能诱导 NPC 说奇怪的话吗?**
A: 玩家不能直接对话 NPC——只能通过神明干预。这个设计一开始是为了叙事(神视角),后来发现顺便屏蔽了 90% 的对抗性 prompt 注入。

## B.6 规模数据(随时可引用)

- **代码**:12,000 行 Python,72 个模块,9 个 pytest 套件
- **测试**:story 模块 34 个用例,记忆系统、事件系统各单独覆盖
- **内容**:14 NPC × 6 张立绘 + 15 头像 + 11 场景背景
- **叙事**:3 条弧线 12 节拍(W1-W2 已完成)
- **文档**:10 份系统策划案 + 总策划案 v1.9

## B.7 反思 / 教训类

**Q: 项目最难的地方是什么?**
A: **让创意涌现和主线可控共存**。早期让 NPC 完全自主,结果 14 个 Agent 各干各的没有主线;后来加硬编码时间表又变回传统 CSV 游戏。折中方案是节拍表 + 编剧 Agent 三档决策,把"该发生的事"和"什么时候发生"解耦。这个抽象花了三次迭代才稳定。

**Q: 有什么想优化的?**
A:
- 编剧 Agent 现在只能触发预定义节拍,还不能即兴创作全新事件(需要新增 `spontaneous_events` 输出字段)
- 多 NPC 并发调用在 3+ S-tier 时会撞 Semaphore 上限
- W3-W9 大纲数据还未补齐(策划工作量瓶颈)
- 敏感内容的运行时强制校验目前是软约束,未在代码层做硬检查

**Q: 如果重来,你会怎么做?**
A: 会更早做"规则/AI 边界文档"。前两个月没有明确边界,LLM 直接写数值 delta 导致平衡崩了,回滚一次。后来把边界文档做在最前面,后面的代码结构自然清爽了。

## B.8 岗位定向说法

| 目标岗位 | 强调重点 |
|---|---|
| **AI Agent 工程师** | 多 Agent 编排、Prompt 工程、LLM 集成、降级链、JSON 解析 |
| **游戏系统策划** | 系统设计能力、10 份策划文档、AI 边界定义、内容克制策略 |
| **游戏程序** | 12000 行 Python、异步架构、WebSocket、连接池、httpx |
| **Tech Artist / 独立开发** | 前后端联调、Godot、成本控制、多层降级 |
| **大厂 AI Lab** | 探索 LLM 驱动内容生成在游戏中的可控性边界,给出 7 条边界原则 |

## B.9 项目关键词(SEO / 关键词密度用)

`多 Agent 系统` · `LLM 编排` · `Prompt 工程` · `涌现叙事` · `AI 原生游戏` · `Anthropic Claude API` · `llama.cpp` · `负载均衡` · `故障转移` · `指数退避` · `Semaphore` · `JSON 容错解析` · `AI 边界设计` · `记忆架构` · `Godot 4.4` · `FastAPI` · `WebSocket` · `asyncio` · `pytest` · `神明模拟` · `因果推演`

---

# 附录 C:三级降级链的项目内实现(深挖)

## C.0 面试提问原文

> **Q:** 三级调用降级链(优先级从高到低):
> 1. 第一级:商用 API(Anthropic),效果最优;
> 2. 第二级:本地 llama.cpp 多实例负载均衡,多端点轮询、信号量限制并发、故障自动切换端点、指数退避重试;
> 3. 第三级:硬编码规则模板兜底,前两级全部失效时,直接预定义逻辑输出,保证游戏不卡死。
>
> 在这个项目中是如何实现的?

## C.1 一句话回答

**统一接口 + 环境变量选一个 LLM 后端 + 空返回契约触发规则兜底。** 三级不是"调用栈叠加",而是:一个 `BaseLLMClient` 抽象接口把 Anthropic 和 llama.cpp 两种实现"选一个"用;两者的失败契约都是**返回 `None` 而不是抛异常**;消费者(NPCAgent、Screenwriter、Narrator)在收到 `None` 时各自触发硬编码兜底。

## C.2 整体架构

```
┌────────────────────────────────────────────────────────┐
│ BaseLLMClient (interface.py:12)                        │
│   async def chat(messages) -> Optional[str]            │
│   契约:成功返回文本,失败返回 None(不抛异常)         │
└────────────────────────────────────────────────────────┘
              ▲
              │ 通过环境变量 LLM_BACKEND 选一个具体实现
              │
    ┌─────────┴─────────┐
    │                   │
Tier 1              Tier 2
AnthropicClient    LoadBalancedClient
                       │
                       │ chat() 返回 None 时,调用方触发 Tier 3
                       ▼
                Tier 3:调用方的 `_default_*()` 硬编码兜底
                     (agent.py:193, screenwriter.py:74)
```

**关键设计:** Tier 3 不是另一个 LLM 客户端,而是每个"消费者"(NPCAgent / Screenwriter / Narrator)自己写的硬编码分支。这样兜底句可以贴合上下文(NPC 兜底用日程模板,编剧兜底走 CSV 事件表)。

## C.3 Tier 1 — AnthropicClient(商用 API)

**代码:** `src/backend/ai/llm_client/anthropic_client.py`
**选择开关:** `state.py:29` — `LLM_BACKEND=anthropic` 时启用
**端点:** 火山方舟 `https://ark.cn-beijing.volces.com/api/coding`(Anthropic Messages 协议兼容)

**关键实现点:**

| 点 | 代码位置 | 作用 |
|---|---|---|
| 协议转换 | `_split_system()` (line 229) | OpenAI 风格 messages → Anthropic 顶级 `system` + `messages` |
| 全局并发上限 | `Semaphore(concurrency=4)` (line 79) | 整个客户端最多 4 个并发 |
| 状态码分类 | line 146-162 | 200 → 返回;429/5xx → 退避重试;其他 4xx → 直接失败(token/参数错) |
| 指数退避 | line 137, 154 | `await asyncio.sleep(1.5 ** (retry+1))`,最多 2 次 |
| 网络异常处理 | line 134 | `TimeoutException/ConnectError/RemoteProtocolError` 都走退避重试 |
| 失败契约 | line 140, 144, 157, 162, 165 | **所有失败路径都返回 None**,不抛异常 |
| 连接池复用 | `_ensure_client()` (line 204) | `httpx.AsyncClient` + `max_keepalive_connections=8`,懒初始化 |

## C.4 Tier 2 — LoadBalancedClient(本地 llama.cpp 负载均衡)

**代码:** `src/backend/ai/llm_client/load_balanced.py`
**选择开关:** `LLM_BACKEND=llama`(默认)

**关键实现点:**

| 点 | 代码位置 | 作用 |
|---|---|---|
| 端点状态 | `EndpointState` (line 42) | 每个端点独立 `Semaphore` + 失败计数 |
| 轮询指针 | `_next_index(exclude)` (line 190) | `self._counter % len(endpoints)`,跳过 `tried` 集合 |
| 故障转移 | `chat()` 主循环 (line 122) | `tried: set[int]`,失败端点入集合,下轮 `_next_index` 自动跳过 |
| 并发保护 | `async with endpoint.semaphore` (line 129) | 单端点最多 2 个并发,防 llama.cpp 显存 OOM |
| 排队超时 | `asyncio.wait_for(_do(), timeout=self._timeout)` (line 135) | 信号量排队太久也算端点失败 |
| 端点内重试 | `_do_request()` (line 226) | 429/503 时 `wait = 1.5**(retry+1)` 秒退避 |
| 连接池 | line 185 | `max_keepalive_connections = len(endpoints)*2` |

**为什么用 tried 集合而不是简单 for 循环?** 因为 `_next_index` 会自动跳过已失败端点,避免撞同一坏端点两次,失败一个端点损失的只是一次探测,不是一次超时。

## C.5 Tier 3 — 规则兜底(分散在各消费者)

前两级都是"调用 LLM 拿文本",Tier 3 是"拿不到文本时用什么"。**没有统一的兜底模块**,每个消费者知道自己该说什么:

**NPCAgent 行动兜底** (`agent.py:142`):
```python
action = await self.llm.chat(messages)
if not action:
    action = self._default_action(day, slot)   # ← Tier 3
```
`_default_action()` (line 193) 按时段 + 当前位置返回模板句:
```python
defaults = {
    Slot.MORNING: f"在{loc}开始新的一天。",
    Slot.NOON: f"在{loc}度过午后时光。",
    Slot.NIGHT: f"在{loc}休息,为明天做准备。",
}
```

**NPCAgent 对话兜底** (`agent.py:189` / `_default_response`):按感性值分支:
```python
return "……(沉默)" if self.static.personality.sensibility < 0.4 else "嗯。"
```

**Screenwriter Agent 兜底** (`screenwriter.py:74`):
```python
if story_outline is None or llm is None:
    return False, []   # 返回 ok=False
```
调用方 `ws_game.py:161` 收到 `screenwriter_ok=False` 后,退回 CSV `match_events()` 硬编码事件表——**这是"整个 AI 编排系统失败后的 Tier 3"**。

**JSON 三层解析兜底** (`screenwriter.py:_parse_llm_output`) — Tier 3 的"内层"版本:LLM 返回文本但格式坏了时,`json.loads` → 正则 `{...}` → 正则 ` ```json``` `,全失败返回 `None` → 上层退回 CSV。

**StubLLMClient** (`interface.py:35`):测试和 headless 环境的"零 LLM"实现,`chat()` 直接返回 `None`,强制走 Tier 3。用于:
- 单元测试(不联网)
- `NPCAgent.__init__` 默认值(`llm or StubLLMClient()`,agent.py:60)

## C.6 三层协作的实际流程(以 NPC 一次 think() 为例)

```
NPCAgent.think(day, slot)
  │
  ├─→ self.llm.chat(messages)              [调用注入的客户端]
  │       │
  │       ├─ (Tier 1) AnthropicClient
  │       │     │ Semaphore(4) 排队 → HTTP POST → 200 返回文本
  │       │     │ 429/5xx → 1.5^n 退避重试 2 次
  │       │     │ 全失败 → return None
  │       │     │
  │       │  或 (Tier 2) LoadBalancedClient
  │       │        │ _next_index() 挑端点 → per-endpoint Semaphore(2)
  │       │        │ HTTP POST 429/503 → 端点内退避重试
  │       │        │ 端点整体失败 → 加入 tried,轮下一个
  │       │        │ 所有端点耗尽 → return None
  │       │
  │       └─→ 返回 str 或 None
  │
  └─→ if not action:                       [Tier 3 触发点]
          action = self._default_action(day, slot)
          # 硬编码模板:"在 {loc} 度过午后时光。"
```

## C.7 三个刻意的设计选择

**1. 失败契约统一为 `Optional[str]`,不抛异常**
把降级决策权留给调用方,避免异常穿透污染业务代码。上层 `if not action` 一行搞定,不需要 `try/except`。

**2. Tier 1/2 是"选一个",不是"叠加"**
Anthropic 挂了不会自动切 llama.cpp。两个都通过同一接口,靠 env 切换。理由:
- 开发期需要频繁在"高质量慢"和"低质量快"之间切,一个 env 变量最省心
- 若做自动切换,需引入健康检查、切换阈值、切回策略,复杂度不匹配 MVP 阶段收益
- 生产阶段可以再包一层"MetaClient"让两个 tier 联动,当前不做

**3. Tier 3 分散实现**
每个消费者知道自己的兜底该说什么,比一个通用兜底库更贴合上下文——NPC 兜底句是"在 X 度过午后",编剧兜底是走 CSV 事件表,叙事润色兜底是直接拼接事件描述。**通用兜底 = 平均质量差,专用兜底 = 每层都合理**。

## C.8 面试速答版(30 秒)

> "我们的三级降级链核心是一个 `BaseLLMClient` 抽象接口,契约是失败返回 None 不抛异常。Tier 1 是 Anthropic 兼容端点(火山方舟),Tier 2 是本地 llama.cpp 负载均衡——多端点轮询、per-endpoint Semaphore 防 OOM、tried set 做故障转移、指数退避 1.5^n。Tier 1/2 通过环境变量 `LLM_BACKEND` 选一个用。Tier 3 不是统一模块而是分散在各消费者:NPC 拿不到 LLM 输出就走日程模板,编剧 Agent 走 CSV 事件表,JSON 解析失败也退回 CSV。这样任何一层挂掉游戏都能通关,LLM 是锦上添花不是命门。"

