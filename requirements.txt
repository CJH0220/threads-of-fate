# 《命运的织线》Python 后端依赖
# 安装方式：
#   conda env create -f environment.yml        (推荐，创建独立环境)
#   pip install -r requirements.txt             (已有环境追加)

# Web 框架
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# 数据模型
pydantic>=2.0,<3.0

# 通信
websockets>=12.0
httpx>=0.27,<1.0      # 异步 HTTP（LLM API 调用）

# 配置
python-dotenv>=1.0

# 测试
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
